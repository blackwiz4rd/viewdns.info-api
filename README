This awesome site called: http://viewdns.info/ has a bunch of DNS tools.  They also provide an API.  


Add your API-KEY to conf-sample and run `mv conf-sample conf`
run the script with: bash viewdns.bash


You can also add options as a parameter

